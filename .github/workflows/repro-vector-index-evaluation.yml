name: Vector index evaluation repro

on:
  push:
    branches:
      - repro/vector-index-evaluation
  workflow_dispatch:

permissions:
  contents: read

jobs:
  execution-repro:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.96.0
      - uses: Swatinem/rust-cache@v2
      - name: Generate current lowering evidence
        env:
          UPDATE_EXPECT: "1"
          TEST_FILTER: "vector_index_evaluation_semantics.leo"
        run: cargo test -p leo-passes --locked -- storage_lowering_runner_test
      - name: Verify a clean rerun
        env:
          TEST_FILTER: "vector_index_evaluation_semantics.leo"
        run: cargo test -p leo-passes --locked -- storage_lowering_runner_test
      - name: Publish observed output
        if: always()
        run: test -f tests/expectations/passes/storage_lowering/vector_index_evaluation_semantics.out && sed -n '1,320p' tests/expectations/passes/storage_lowering/vector_index_evaluation_semantics.out >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vector-index-evaluation-repro
          path: tests/expectations/passes/storage_lowering/vector_index_evaluation_semantics.out
